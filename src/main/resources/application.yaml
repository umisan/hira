spring:
  application:
    name: hira
  datasource:
    url: jdbc:h2:file:./data/hira;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
